{
  "name": "htmx-rcon-vite",
  "version": "1.0.0",
  "private": true,
  "description": "Vite + htmx RCON console. API routes + SSE. Deployable to Vercel.",
  "scripts": {
    "dev": "concurrently -k \"node dev-server.cjs\" \"wait-on tcp:3001 && vite\"",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "api": "node dev-server.cjs"
  },
  "dependencies": {
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^6.0.0",
    "wait-on": "^8.0.1"
  }
}
